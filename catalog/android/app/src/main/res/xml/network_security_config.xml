<?xml version="1.0" encoding="utf-8"?>
<!-- The Instant / AI Assistant examples connect to a LOCAL dev Document
     Engine / AI server over plain http. The Android emulator reaches the
     host via 10.0.2.2, which is not loopback-exempt (unlike localhost on
     iOS, where ATS exempts loopback). Permit cleartext for those dev hosts
     only — everything else keeps the platform default (no cleartext). -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
